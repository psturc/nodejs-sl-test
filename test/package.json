{
    "scripts": {
        "test:e2e": "mocha e2e.test.js --reporter mocha-junit-reporter",
        "test:api": "mocha e2e.test.js --grep 'Backend API' --reporter mocha-junit-reporter",
        "test:frontend": "mocha e2e.test.js --grep 'Frontend Integration' --reporter mocha-junit-reporter"
    },
    "devDependencies": {
        "axios": "^1.7.9",
        "chai": "^4.5.0",
        "mocha": "^10.8.2",
        "mocha-junit-reporter": "^2.2.1",
        "puppeteer": "^23.10.4",
        "supertest": "^6.3.4"
    }
}
